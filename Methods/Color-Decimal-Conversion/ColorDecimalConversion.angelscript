/* Convert a single value to decimal */
float ColorDecimalConversion(float color){
	return color = color / 255.0f;
}

/* Convert a vector3 (RGB) to decimal */
vector3 ColorDecimalConversion(vector3 color){
	return color = color / 255.0f;
}